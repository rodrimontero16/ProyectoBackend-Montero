<nav>
    <ul>
        <li><strong>Ecommerce</strong></li>
    </ul>
    <ul>
        {{#if isAdmin}}
            <li><a href="/api/products">ProductsManager</a></li>
            <li><a href="/api/carts">CartsManager</a></li>
            <li><a href="/products">Tienda</a></li>
        {{else}}
            <li><a href="/products">Tienda</a></li>
        {{/if}}
        <li><a href="/api/auth/logout">Cerrar sesión</a></li>
    </ul>
</nav>
<article>
    <header>
        <h1>Datos del usuario:</h1>
    </header>
    <p>Nombre y apellido: {{user.fullName}}</p>
    <p>Correo: {{user.email}}</p>
    <p>Permisos: {{user.role}}</p>
</article>
<a href="#">
    <form method="POST" action="/api/users/premium/{{user.id}}">
        <button type="submit">Cambiar rol de usuario</button>
    </form>
</a>